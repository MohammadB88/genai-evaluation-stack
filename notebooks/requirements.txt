# Fallback to public PyPI for packages not on this environment's configured
# default index (e.g. a corporate/internal mirror) — needed if the default
# index doesn't carry every package below.
--extra-index-url https://pypi.org/simple

# Model-quality evaluation notebook (model_eval_deepeval.ipynb):
# deepeval provides the LLM-as-judge metrics; openai is the client used to
# query the model under test (any OpenAI-compatible endpoint). Pinned to the
# same version as requirements.txt (src/) — DeepEval's evaluate()/metric APIs
# have changed across releases, and notebook/src drift here previously caused
# a TypeError on evaluate()'s error-handling kwarg.
deepeval==4.1.1
openai>=1.0

# MLflow logging notebook (model_eval_deepeval_mlflow.ipynb):
mlflow>=3.0

# Phase 4 prototypes — exploratory only, not part of the Phase 1 core stack
# (see CLAUDE.md's trigger-based additions). Pinned loosely; verify against
# installed-version docs before relying on any API shown in these notebooks.
langfuse>=3.0   # phase4_langfuse_prototype.ipynb
evidently>=0.7  # phase4_evidently_prototype.ipynb
pandas>=2.0
